The perfect plan:


------BACKGROUND PROMPT GENERAL-----------

Act as an expert React developer. I need a global animated background component. We are building a custom 2D physics simulation for floating Roman numerals.

Global Rules:

Deep midnight bluish-purple theme.

Absolutely NO sharp corners in any UI element. Everything must be highly rounded.

1. Base Container Setup:
Create a component that is fixed, inset-0, with pointer-events-none and z-[-1]. The base background must be a very dark, rich radial gradient fading into pure black (e.g., bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-[#110b29] to-[#000000]). Apply overflow-hidden so floating elements never cause scrollbars.

2. The Physics Engine Setup (CRITICAL):

Do not use standard Framer Motion declarative animate props for movement.

Instead, create a useRef to hold an array of 40 particle objects.

Each particle needs: id, text (a Roman numeral like I, V, X, L, C, D, M), x, y, vx, vy, radius (bounding box size), type (either 'small' for values 1-12, or 'large' for 13+), and active (boolean, default true).

Randomize the text sizes (from text-sm to text-4xl) for parallax depth.

3. The Animation Loop (requestAnimationFrame):
Write a requestAnimationFrame loop to update the physics every frame.

Movement: Add vx to x, and vy to y for every active particle.

Wall Bounces: If a particle hits the edge of the viewport, reverse its velocity (vx *= -1 or vy *= -1) so it stays on screen.

4. The Collision Logic (Distance Checking):
Inside the frame loop, check the distance between every pair of active particles using Math.hypot(dx, dy). If the distance is less than their combined radii:

Condition A (Bounce): If particle1.type === particle2.type (both small or both large), reverse their velocities so they bounce off each other.

Condition B (Annihilation): If particle1.type !== particle2.type (one small, one large), they destroy each other. Set active = false for both.

Update a React state with the active particles to trigger a re-render.

5. Rendering & Styling:

Map over the particles. If !active, render nothing. Render active particles as absolutely positioned div elements. Font must be clean sans-serif.

Small Numerals (1-12): Color them crisp white or icy blue. Apply a soft ambient CSS pulse animation (fade in and out).

Large Numerals (13+ GLITCH): Apply a harsh CSS glitch effect using a keyframe animation for instant jitter.

CRITICAL GLITCH COLORS: Do NOT use magenta, pink, or red. The glitch must only use white, black, and icy blue. Use a text-shadow like this to create the visual tear: text-shadow: 2px 0px 0px #ffffff, -2px 0px 0px #3b82f6, 0px 2px 0px #000000;. Use hard keyframes, no smooth easing.




-----------DEFAULT NON HOVER ANIMATION PROMPT-----

Act as an expert React developer using Framer Motion and Tailwind CSS. Build a looping typography animation for the Hero section. 

Global Rules: Deep midnight bluish-purple theme (bg-[#0b0914]). NO sharp corners anywhere. Do not use `scale` for morphing shapes.
CRITICAL INSTRUCTION: DO NOT use `useState`, `useEffect`, `setInterval`, or `setTimeout`. The looping will be handled entirely via Framer Motion's `repeat: Infinity`.

1. Layout & Text Update:
The main outer wrapper must be `relative flex items-center justify-center min-h-[200px] w-full bg-[#0b0914] rounded-[40px] overflow-hidden`.
Inside it, create an inner `relative flex items-center justify-center` container.
The headline consists of two parts.
Part 1 (Static text): "The unfinished stories that " (wrap in a normal div, z-10).
Part 2 (Animated text): "could have changed everything" (wrap in a motion.div with the `layout` prop, z-20, whitespace-nowrap, rounded-full, borderWidth: "2px", borderStyle: "solid").
Both should use large, bold, crisp white typography, positioned so it reads as a single sentence.

2. The Morphing Animation (4-Step Keyframe Arrays):
Instead of React state, pass these exact 4-step arrays directly into the `animate` prop of Part 2. 
This maps to: [Start Idle, End Idle (Hold), Thin Line, Wrapper Pill Box].

animate={{
  color: ["#ffffff", "#ffffff", "rgba(255,255,255,0)", "rgba(255,255,255,0)"],
  backgroundColor: ["rgba(255,255,255,0)", "rgba(255,255,255,0)", "#ffffff", "rgba(255,255,255,0)"],
  height: ["auto", "auto", "4px", "120%"],
  width: ["auto", "auto", "auto", "105%"],
  borderColor: ["rgba(255,255,255,0)", "rgba(255,255,255,0)", "rgba(255,255,255,0)", "#ffffff"],
  position: ["relative", "relative", "relative", "absolute"],
  left: ["auto", "auto", "auto", "50%"],
  x: ["0%", "0%", "0%", "-50%"],
  y: ["0%", "0%", "0%", "-50%"]
}}

3. The Infinite Loop Transition:
Apply this exact transition object to ensure it runs on a 5-second continuous loop (holding idle for 3 seconds, then animating for 2 seconds):

transition={{ 
  duration: 5, 
  times: [0, 0.6, 0.76, 1], 
  repeat: Infinity, 
  repeatType: "loop",
  ease: "easeInOut" 
}}

-------------ON HOVER ANIMATION PROMPT--------------



Act as an expert React developer using Framer Motion and Tailwind CSS. We are building the Hero Typography "Clockwork" component. The previous attempt broke because the text rotated in the idle state and the transform origins were mathematically misaligned. 

Global Rules: Deep midnight bluish-purple theme. NO sharp corners. 

1. Layout & State Setup:
- Main container: `relative w-full h-[400px] flex items-center justify-center cursor-pointer overflow-hidden`.
- State: `isHovered` boolean triggered by `onMouseEnter` / `onMouseLeave`.
- Sentence Flow: In the idle state, the text "The unfinished stories that changes everything" MUST read normally as an inline, centered sentence. Use a flex wrapper for the words.

2. The Clock Face ("The unfinished stories that "):
- Add the `layout` prop. Wrap in `<motion.div>`.
- Idle (isHovered: false): Render normally as part of the sentence flow. `position: "relative"`, `color: "#ffffff"`, `border: "0px solid transparent"`, `width: "auto"`, `height: "auto"`, `borderRadius: "0px"`, `x: 0`, `y: 0`, `top: "auto"`, `left: "auto"`.
- Hover (isHovered: true): Break out of flow and morph into the clock outline. Animate to: `position: "absolute"`, `top: "50%"`, `left: "50%"`, `x: "-50%"`, `y: "-50%"`, `color: "transparent"`, `width: "300px"`, `height: "300px"`, `border: "2px solid #ffffff"`, `borderRadius: "50%"`.

3. The Hour Hand ("changes"):
- Add the `layout` prop. Wrap in `<motion.span>`.
- Idle (isHovered: false): Normal text. `position: "relative"`, `color: "#ffffff"`, `width: "auto"`, `height: "auto"`, `rotate: 0`, `x: 0`, `y: 0`, `top: "auto"`, `left: "auto"`. (CRITICAL: Ensure rotate is strictly 0 here).
- Hover (isHovered: true): 
  - Morph: `color: "transparent"`, `backgroundColor: "#ffffff"`, `width: "4px"`, `height: "80px"`, `borderRadius: "999px"`.
  - Position: `position: "absolute"`, `top: "50%"`, `left: "50%"`.
  - Alignment (CRITICAL): Set `x: "-50%"`, `y: "-100%"`, and `originY: 1`. This perfectly anchors the bottom of the hand to the dead center of the clock.
  - Animation: `rotate: [0, 360]` with `transition: { duration: 12, repeat: Infinity, ease: "linear" }`.

4. The Minute Hand ("everything"):
- Add the `layout` prop. Wrap in `<motion.span>`.
- Idle (isHovered: false): Normal text. `position: "relative"`, `color: "#ffffff"`, `width: "auto"`, `height: "auto"`, `rotate: 0`, `x: 0`, `y: 0`, `top: "auto"`, `left: "auto"`.
- Hover (isHovered: true): 
  - Morph: `color: "transparent"`, `backgroundColor: "#ffffff"`, `width: "4px"`, `height: "120px"`, `borderRadius: "999px"`.
  - Position: `position: "absolute"`, `top: "50%"`, `left: "50%"`.
  - Alignment (CRITICAL): Set `x: "-50%"`, `y: "-100%"`, and `originY: 1`.
  - Animation: `rotate: [0, 360]` with `transition: { duration: 2, repeat: Infinity, ease: "linear" }`.


-------------------HOURGLASS STATIC PROMPT-----------------


The Idle Hourglass & Text-Grain Loop Prompt
Act as an expert React developer using Framer Motion and Tailwind CSS. Build a desktop-optimized, centered Hourglass component with a continuous, looping text animation.

Global Rules: Deep midnight bluish-purple background. Absolutely SHARP CORNERS ARE ALLOWED HERE. MAKE A PROPER HOURGLASS..

1. The Layout (Desktop Optimized & Centered):

Create a full-width container (min-h-screen flex items-center justify-center).

The Hourglass Container: A centrally aligned box (e.g., w-[400px] h-[500px] flex justify-between).

The Rods (}{): On the far left and far right, create two vertical, thick pill-shaped divs (w-4 h-full rounded-full bg-[#2a1b3d] shadow-lg). These represent the wooden framing rods.

The Glass: Between the rods, create the hourglass shape. It needs a Top Chamber, a narrow Center Neck, and a Bottom Chamber. (You can use clip-paths, borders, or stacked flex containers to shape this cleanly).

The Sand Pile: At the base of the Bottom Chamber, create a rounded shape (bg-white/80 rounded-t-full) representing the pile of fallen sand.

2. The Data & State:

Store this exact array of strings: ["Devjams 2026", "CodeToCreate 2026", "CodeChef 2026", "Ideathon 2026", "WinHack 2026", "HeartWaves 2026", "SmartIndiaHackathon 2026"].

Create a state variable currentIndex to track which name is currently displaying.

3. The Framer Motion Grain Cycle (CRITICAL):
Build a continuous animation sequence that loops perfectly through the array.

Step 1 (The Drop): A small white dot (w-2 h-2 rounded-full bg-white) spawns in the Top Chamber and animates falling down into the center neck.

Step 2 (The Enlargement): Once it hits the center neck, the dot physically morphs/expands into the current text string (e.g., "Devjams 2026"). The text should be crisp, white, and clearly visible. Hold this text state for 1.5 seconds.

Step 3 (The Shrink & Finish): The text morphs/shrinks back into a single dot, and continues falling from the neck down into the Bottom Chamber, merging into the Sand Pile.

Step 4 (The Loop): The exact moment the text shrinks back into a dot, increment currentIndex so a new dot spawns at the top and repeats the process with the next name.

4. Animation Execution:
Use AnimatePresence with mode="wait" or a well-timed useEffect sequence using Framer Motion's useAnimation controls to ensure the timing of the falling dot, the text expansion, and the text shrinking never overlap or break.


---------------HOURGLASS SCROLLWHEEL PROMPT----------------


Act as an expert React developer using Framer Motion (useScroll, useTransform) and Tailwind CSS. We are taking the existing centered Hourglass component and adding a complex, scroll-driven "shatter and morph" animation that triggers when the hourglass hits the top of the viewport.

Global Rules: Deep midnight bluish-purple theme. SHARP CORNERS ARE ALLOWED. Desktop optimized (use flex/grid for responsive zoom handling).

1. Scroll Container Setup:

Wrap the entire component in a tall container (h-[300vh]) to provide scrolling space.

Make the main hourglass wrapper sticky top-0 h-screen flex items-center justify-center.

Use Framer Motion's useScroll to track progress through this specific component. Map a progress variable from 0.0 to 1.0. The shatter animation should ONLY begin when progress is > 0.

2. The Shatter Transforms (Mapping Scroll Progress):
Bind these specific elements to the scroll progress using useTransform:

Left Rod (Top Border): As the user scrolls, animate rotate to 90deg, move y upwards, and stretch width to 100%. It horizontally forms the top border of a new rectangular UI container.

Right Rod (Right Border): Move x to the far right, and scale height to 100%. It becomes the solid rightmost vertical border.

Top Half Glass (Horizontal Filters): Animate x to the right and opacity to 0. In its place, fade in (opacity: [0, 1]) a row of horizontal filter pills: ALL, Stalled, Abandoned, Built, Open to collab.

Bottom Half Glass & Sand Pile: Animate x to the left, rotate slightly, and fade opacity to 0 to simulate breaking.

The Glass Shard (Divider): Animate a single thin glowing line (w-[2px] bg-white/20) dropping from the center to act as a static vertical divider for the new sidebar.

3. The Grains Morphing into the Sidebar:
As the bottom glass breaks, the "sand grains" transition into the sidebar layout on the left side of the screen. Fade this layout in as scroll progress nears 1.0. Match this exact structure:

DOMAIN SECTION: Header "DOMAIN". Items: All Domains (active highlight), Health Tech, Climate Tech, Developer Tools, FinTech, EdTech.

TRACK SECTION: Header "TRACK". Items: All Tracks (active highlight), AI / ML, Web3 / DeFi, Developer Tools, CLI / Productivity, Healthcare.

Toggle Switch: At the bottom, text Abandoned + Promising with subtext Filter abandoned only, alongside a highly rounded toggle switch component.

4. The Layout Gap:
In the final morphed state, the left side contains the Domain/Track sidebar, the right rod forms the right border, and the left rod forms the top border. CRUCIAL: Leave a large, empty visual gap/flex-space next to the sidebar. This blank space is reserved for a future search/clock component.




-----------------THE CLOCK PROMPT(INFO)-----------------------




Act as an expert React developer using Framer Motion and Tailwind CSS. Build an interactive, circular "Idea Clock" component with a centered Search bar, designed to fit into the blank space next to our filter sidebar.

Global Design Rules:
- Deep midnight bluish-purple theme (#110b29 to #000000), crisp white text, icy blue highlights (#3b82f6).
- NO sharp corners anywhere (rounded-full, rounded-2xl). Everything must feel sleek, rounded, and smooth.

1. Layout & Container Bounds:
- Create a container (relative w-full max-w-[420px] aspect-square flex items-center justify-center p-4).
- Render a subtle, glowing outer clock ring (border border-white/10 rounded-full bg-white/[0.02] backdrop-blur-md w-full h-full relative flex items-center justify-center).
- Ensure the clock contains all elements within its boundaries without text clipping or overflowing.

2. The Center Search Bar:
- Directly in the dead center of the clock face, place a sleek search component.
- Style: relative z-20 w-[65%] h-12 bg-white/10 hover:bg-white/15 border border-white/20 rounded-full flex items-center px-4 backdrop-blur-lg shadow-2xl transition-all.
- Inside: A subtle search icon on the right, an input field with placeholder text "Search archived ideas...", white text, and focus:outline-none.

3. The 12 Circular Idea Slots (Replacing 1-12 Numerals):
- Create a mock array of 24 to 36 user-inputted idea titles (e.g., "AI Code Reviewer", "BioTrack App", "EcoEnergy Grid", "DeFi Vault").
- Calculate the 12 positions around the clock perimeter using polar coordinates (angle = i * 30deg - 90deg).
- Place each idea slot at its respective position (x = radius * cos(angle), y = radius * sin(angle)).
- Style the 12 idea slots as tiny, pill-shaped badges (text-xs text-white/80 bg-white/5 border border-white/10 px-2 py-1 rounded-full truncate max-w-[90px] backdrop-blur-sm).

4. Clock Hands & Dynamic Cycle Logic:
- NO SECOND HAND. Render only an Hour Hand and a Minute Hand anchored at the exact center.
- Minute Hand: Sleek, thin white line (w-[2px] h-[38%] bg-gradient-to-t from-white to-blue-400 rounded-full origin-bottom). Animate rotate smoothly from 0deg to 360deg (e.g., duration of 12 seconds per full revolution).
- Hour Hand: Slightly thicker and shorter line (w-[4px] h-[25%] bg-white rounded-full origin-bottom).
- The Cycle Trigger: 
  - Track the currentCycleIndex in React state.
  - When the Minute Hand completes a full 360-degree circle, advance currentCycleIndex to load the NEXT batch of 12 ideas into the perimeter slots.
  - Simultaneously, advance the Hour Hand by 30 degrees (one hour position).
  - Animate the idea labels with a soft fade transition (AnimatePresence opacity fade) as they swap out for the new batch.


-----------------THE CLOCK PROMPT(SEARCH)-----------------------
Act as an expert React developer using Framer Motion and Tailwind CSS. Extend the "Idea Clock" search component with an advanced search trigger, clock fracture animation, dynamic hand interaction, and an expanded detail card view.

Global Rules:
- Deep midnight bluish-purple theme (#110b29 to #000000), crisp white text, icy blue accents (#3b82f6).
- NO sharp corners on cards or badges (rounded-full, rounded-2xl). Everything must feel sleek, rounded, and smooth.
- NO emojis anywhere in status or collaboration tags. Use clean text or SVG icons.

1. Search Trigger & Clock Break Physics:
- Create state variables: `isSearching` (boolean), `selectedIdea` (object or null), `hoveredIndex` (number or null).
- When a user submits a search query in the center search bar:
  1. Hand Acceleration: Spin the Hour and Minute hands hyper-fast (e.g., rotate: [0, 1440deg] in 0.6s).
  2. Shift & Fracture: Animate the outer clock container sliding to the left side of the screen so only 1/2 or 3/4 of the clock remains visible (x: -35%).
  3. The Crack: Reveal a sharp, radial triangular crack/break opening up on the right side of the clock face using Framer Motion SVG clip-path or absolute polygons with a subtle icy-blue border glow.

2. Search Results Stream & Glitch Numerals:
- Out of the radial crack, animate a scrollable list of search results cascading to the right.
- Item Numbering: Number each item in the list using Roman Numerals (I, II, III... up to XII).
- Glitch Rule for 13+ (CRITICAL): If the result index exceeds 12 (XIII, XIV, XV...), apply the harsh digital glitch effect to the numeral label:
  - Keyframe position/skew jitter.
  - Text shadow tear strictly using white, black, and icy blue: `text-shadow: 2px 0px 0px #ffffff, -2px 0px 0px #3b82f6, 0px 2px 0px #000000;`.
  - Absolutely NO magenta, pink, or red in the glitch effect.

3. Clock Hands Morphing (Pointer & Back Button):
- Minute Hand (The Pointer):
  - Transforms into a crisp directional pointer.
  - As the user scrolls through or hovers over items in the search results list, dynamically rotate the Minute Hand so it points directly at the currently hovered/active list item.
- Hour Hand (The Back Button):
  - Morphs into a stylized left-pointing arrow button (←) anchored on the clock hub.
  - When clicked: Resets search state (`isSearching = false`), spins hands back to idle, closes the crack, and shifts the clock back to full center view.

4. Expanded Detail Card View (OnClick):
- When a user clicks any search result item, open a large, non-fullscreen floating detail modal/card (`w-[650px] max-w-[90vw] bg-[#0c081e]/90 border border-white/15 backdrop-blur-2xl rounded-3xl p-8 shadow-[0_0_50px_rgba(0,0,0,0.8)] z-50 relative`).


----------------------FAVOURITES-DIGITAL-CLOCK--------------------------

Act as an expert React developer using Framer Motion and Tailwind CSS. Build a digital clock metrics component that appears when the user scrolls past the previous Idea Clock search section.

Global Design Rules:
- Deep midnight bluish-purple theme (#110b29 to #000000), crisp white digits, icy blue glow accents (#3b82f6).
- NO sharp corners anywhere (rounded-3xl, rounded-full, pill-shaped panels).
- Clean, modern digital font or mono layout with soft glow text-shadows.

1. Layout & Scroll Entrance:
- Section Container: `min-h-screen w-full flex flex-col items-center justify-center relative z-10 px-4 py-20`.
- Entrance Animation: As the user scrolls into this section, use Framer Motion (`initial={{ opacity: 0, y: 50 }}` whileInView={{ opacity: 1, y: 0 }}`) to smoothly reveal the digital display panel.

2. The Main Digital Clock Display Panel:
- Panel Box: Sleek glassmorphic container (`max-w-2xl w-full bg-[#150d33]/80 border border-white/10 backdrop-blur-2xl rounded-3xl p-8 sm:p-12 shadow-[0_0_50px_rgba(59,130,246,0.15)] flex flex-col items-center gap-6`).

3. Primary Display (Replacing Hour & Minute Digits):
- Function: Displays the total "Number of Ideas Submitted" formatted strictly across 4 digital slots (e.g., `0 4 8 2`).
- Layout & Styling:
  - Flex row with subtle colon or divider separators, mimicking a digital clock layout (`HH : MM` layout replaced by 4 digits).
  - Digits styled with large, crisp, glowing typography (`text-6xl sm:text-8xl font-mono font-bold text-white drop-shadow-[0_0_15px_rgba(255,255,255,0.8)]`).
  - Label below: A pill badge reading `TOTAL IDEAS SUBMITTED` (`text-xs uppercase tracking-widest text-blue-400 bg-blue-500/10 border border-blue-500/20 px-4 py-1.5 rounded-full`).
- Animation: On viewport entry, animate the digits rolling up rapidly from `0000` to the target count (e.g., `0482`).

4. Secondary Display (Replacing the Seconds Counter):
- Function: A static digital counter at the bottom displaying the "Number of Ideas Favourited".
- Layout & Styling:
  - Positioned at the bottom center of the digital display box.
  - Smaller digital readout (`text-2xl sm:text-3xl font-mono font-semibold text-blue-300 drop-shadow-[0_0_10px_rgba(59,130,246,0.6)]`).
  - Label next to or under the number: `IDEAS FAVOURITED` with a subtle heart icon outline (no red/pink; use icy blue `#3b82f6` or crisp white).
  - Styled inside a recessed, pill-shaped display bar (`bg-black/40 border border-white/10 px-6 py-2 rounded-full flex items-center gap-3`).

5. Auxiliary Action Buttons (Right Side of Clock Panel):
- Adjacent to the right side of the main digital clock panel (or integrated into its right edge), create a vertical flex column of two interactive action buttons.
- Styling: Glassmorphic pill buttons (`bg-[#150d33]/50 hover:bg-white/10 border border-white/10 text-white text-sm font-medium rounded-full px-5 py-3 flex items-center gap-3 transition-all backdrop-blur-md shadow-lg`). 
- Button 1 (Favourites):
  - Function: Expands the list of what the user has favourited.
  - Content: SVG Star outline icon + text "View Favourites".
- Button 2 (Responses):
  - Function: Opens a view of favourited ideas that have received responses from their original owners.
  - Content: SVG Message/Reply outline icon + text "Owner Responses".
  - Add a subtle glowing cyan notification dot (`w-2 h-2 rounded-full bg-cyan-400 shadow-[0_0_8px_rgba(34,211,238,0.8)]`) to this button to simulate active responses.

6. User Dashboard Trigger (Top Right Corner):
- Layout: Position this button absolutely in the top right corner of the screen/section (`absolute top-8 right-8 z-50`).
- Styling: A highly polished, glowing pill button (`bg-gradient-to-r from-[#1e1445] to-[#110b29] border border-cyan-500/30 hover:border-cyan-400 text-white px-6 py-2.5 rounded-full flex items-center gap-3 shadow-[0_0_20px_rgba(56,189,248,0.15)] transition-all cursor-pointer`).
- Content: SVG User/Folder icon + text "My Ideas".
- Notification Badge: Include a small counter badge (e.g., a red or cyan circular badge reading "3") overlapping the corner of the button to indicate pending collaboration requests or notifications.
- Function: State that clicking this button toggles a state (`isDashboardOpen`) which will eventually open a template/modal showing the user's submitted ideas, system notifications, and incoming requests from others to collaborate.


----------------------TEMPLATE VISUALISATION+INTERACTION----------------


Act as an expert React developer using Framer Motion and Tailwind CSS. Build a standalone, floating Detail Card Modal component (`IdeaDetailModal.tsx`) that opens when an idea from our search results list is clicked.

Global Design Rules:
- Deep midnight bluish-purple theme (#0c081e), crisp white text, cyan (#38bdf8) and icy blue accents.
- NO sharp corners anywhere (rounded-3xl, rounded-xl). Everything must feel sleek, rounded, and smooth.
- NO emojis anywhere in status or collaboration tags. Use SVG icons or clean text.

1. Modal Backdrop & Container:
- Overlay: Fixed backdrop blur (`fixed inset-0 bg-black/60 backdrop-blur-md z-50 flex items-center justify-center p-4`).
- Card Box: Centered floating card (`w-[650px] max-w-[95vw] bg-[#0c081e]/95 border border-white/15 rounded-3xl p-8 shadow-[0_0_60px_rgba(0,0,0,0.9)] relative overflow-hidden`).
- Entrance Animation: Smooth scale & fade transition (`initial={{ scale: 0.9, opacity: 0 }}` `animate={{ scale: 1, opacity: 1 }}`).

2. Top Bar & Header Layout:
- Top Left: Status Dot + Track + Domain tags styled as uppercase mono text with subtle dot separators (e.g., "🔴 ABANDONED · AI/ML · SMART CITIES").
  - "Built" -> Glowing Emerald Green dot & text.
  - "Stalled" -> Glowing Amber Yellow dot & text.
  - "Abandoned" -> Glowing Rose Red dot & text.
- Top Right: Sleek close button (`✕`) with hover background transition.

3. Title, Description & Tech Stack:
- Large, bold project Title (e.g., "NeuralRoute").
- Clean description paragraph below explaining the core concept.
- Row of uppercase tech pill badges (`bg-[#111c2e] border border-cyan-500/30 text-cyan-400 text-xs font-mono font-semibold px-3 py-1.5 rounded-xl uppercase tracking-wider`).

4. Exact Roadblock Container (Visual Highlight):
- Highlighted callout box with a subtle glowing border matching the status color (`bg-rose-950/20 border border-rose-500/30 rounded-2xl p-4 my-5`).
- Small glowing red indicator dot with uppercase text `EXACT ROADBLOCK`.
- Body paragraph detailing why the project was stalled or abandoned.

5. Bottom Bar Actions:
- Bottom Left: Display Team Name & Year in subtle muted text (e.g., "Team Sigma · 2026").
- Bottom Right Actions:
  - Collaboration Status Badge (Strictly NO emojis):
    - Open to Collab: Green rectangular badge (`bg-emerald-500/15 border border-emerald-500/30 text-emerald-400 rounded-lg px-3 py-2 flex items-center gap-1.5 text-xs font-semibold`) featuring SVG checkmark `✓ Open to Collab`.
    - Not Open to Collab: Red rectangular badge (`bg-rose-500/15 border border-rose-500/30 text-rose-400 rounded-lg px-3 py-2 flex items-center gap-1.5 text-xs font-semibold`) featuring SVG cross `✕ Not Open to Collab`.
  
  - FAVORITE ACTION BUTTON:
    - Interactive toggle button with a Star icon (`flex items-center gap-2 px-5 py-2.5 rounded-2xl font-medium transition-all shadow-lg cursor-pointer`).
    - Unfavourited State: `bg-white/10 hover:bg-white/20 border border-white/20 text-white` with outlined SVG Star `★ Favourite Idea`.
    - favourited State: Glowing gradient background (`bg-gradient-to-r from-cyan-400 to-indigo-600 text-white shadow-[0_0_20px_rgba(56,189,248,0.5)]`) with filled Star `★ Idea Favourited`.
    - Functionality: Toggling this updates the local idea state and increments/decrements the global `favouritedIdeasCount` state variable.
